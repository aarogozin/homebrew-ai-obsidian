# Homebrew Tap for AI Obsidian

Install the AI Obsidian CLI:

```bash
brew tap aarogozin/ai-obsidian
brew install ai-obsidian
ai-obsidian init
```

This tap installs only the CLI. Runtime setup for Obsidian, oMLX, models, plugins, and voice dependencies stays inside `ai-obsidian init`.
